export global Control {

    //metronome def
    in-out property <int> numerator; //that's what is above in signature
    in-out property <int> denominator; //that's what is below in signature
    in-out property <int> bpm;
    in-out property <int> actualBeat;

}